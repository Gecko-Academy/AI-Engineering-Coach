[project]
name = "gecko-ai-coach"
version = "0.1.0"
description = "Ask a course a question, measure what answers it, and improve it."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
keywords = ["retrieval", "rag", "education", "harness", "evaluation"]
# No runtime dependencies, on purpose: a tool people are asked to fork should
# install anywhere, in one step, with nothing to build.
dependencies = []

[project.scripts]
ai-coach = "gecko_ai_coach.cli:main"

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.6", "mypy>=1.11"]
vector = ["chromadb>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gecko_ai_coach"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
